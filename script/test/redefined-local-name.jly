(define > (lambda [n] true))
(define + (lambda [n] 0))
(define cons (lambda [h t] ()))

(define make-num-seq
  (lambda [fn n]
    (if (> n 100)
      (define fn "+++++++")
      (cons n (make-num-seq (fn n))))))

(define num-seq (make-num-seq (lambda [n] (+ 10 n)) 0))
